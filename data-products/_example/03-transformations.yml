# Specify and describe any transformations or cleansing the data has undergone.
# The types of cleansing we recognise are described in our documentation.
cleansing:
  population_by_offence:
    type: "none"
  population_by_age:
    type: "none"
  population_by_prison:
    type: "none"
    columns:
      Prison:
        - type: "standardisation"
          description: "Prison names can be inconsistent between systems and publications, so we have replaced prison names with the standard set"
transformations:
  population_by_offence:
    type: "none"

  